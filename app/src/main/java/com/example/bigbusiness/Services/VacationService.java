package com.example.bigbusiness.Services;

import com.example.bigbusiness.Models.Vacation;

import java.util.HashMap;
import java.util.List;

public class VacationService {
    String totalVacationDays;
    String remainingDays;
    HashMap<String, List<Vacation>> vacationHistory; // key - emp_Id, values - dates
//    addVacation(Vacation);
//    getNumberOfLeaves(month);
//    addEmployee(empId); // add entry in hashmap with empty list
//    addVacation(empId, (Vacation)vacation);
}
